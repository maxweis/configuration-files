#include <stdio.h>
#define file argv[1]

int main(int argc, char *argv[])
{
	FILE *input;

	/*printf("Open file: ");*/

	/*char file[50];*/

	scanf("%s", file);

	input = fopen(file, "r");

	char c;
	while ((c = getc(input)) != EOF)
		printf("%c", c);

	printf("\n");

	return 0;
}
