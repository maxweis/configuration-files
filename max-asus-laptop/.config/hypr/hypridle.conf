# Dim the OLED before Omarchy's screensaver starts.
general {
  lock_cmd = :
  before_sleep_cmd = :
  after_sleep_cmd = :
}

listener {
  timeout = 90
  on-timeout = brightnessctl -s set 35%
  on-resume = brightnessctl -r
}
